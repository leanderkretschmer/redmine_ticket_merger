<% if @can[:merge] && @issues.size == 1 %>
  <li class="folder">
    <a href="#" class="submenu"><%= l(:label_merge_issue) %></a>
    <span class="icon-only"><%= sprite_icon('angle-right', rtl: true) %></span>
    <ul style="display: none;">
      <li>
        <%= link_to l(:label_merge_issue), merge_form_issue_path(@issues.first), :class => 'icon icon-merge' %>
      </li>
    </ul>
  </li>
<% end %>
